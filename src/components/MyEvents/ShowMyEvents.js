import React from 'react'
// import Loading from '../Loading/Loading';

export default function ShowMyEvents(props) {
    // if (props.ready) return (
        <>
            {props.showMyEvents}
        </>
    //     )
    // else return <Loading />;
}
